/target
/data/*.db
/data/*.db-wal
/data/*.db-shm
.env
.DS_Store
.cursor/
.claude/
.zed/
.gstack/
gory_coolify_access_token.txt
data/load-tests/
tools/wasm-bench/bare/

# Build-time Tailwind output — regenerated by `bun run build` (local)
# or the css-builder Docker stage (production).
static/app.css
node_modules/
