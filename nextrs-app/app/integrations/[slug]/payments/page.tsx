import { Card, Empty, IntegrationBoundary, Page, date, money } from "@trust-deeds/client";

export default function Payments() { return <IntegrationBoundary>{data => <Page title="Payments" description={`The 100 most recent imported payments from ${data.connection.name}.`}>{data.payments.length === 0 ? <Empty title="No payments" description="Payments will appear after the provider has been synced." /> : <Card className="overflow-x-auto"><table className="data-table"><thead><tr><th>Date</th><th>Borrower</th><th>Loan</th><th>Check</th><th>Interest</th><th>Principal</th><th>Total</th></tr></thead><tbody>{data.payments.map(payment => <tr key={payment.id}><td>{date(payment.check_date)}</td><td className="font-medium">{payment.borrower_name}</td><td className="font-mono text-xs">{payment.loan_account}</td><td>{payment.check_number || "—"}</td><td>{money(payment.interest)}</td><td>{money(payment.principal)}</td><td className="font-medium text-primary">{money(payment.amount)}</td></tr>)}</tbody></table></Card>}</Page>}</IntegrationBoundary>; }
